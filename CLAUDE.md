# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty scaffolding: the only file is `README.md`, containing just the title `# Test`. There is no source code, no build/test/lint tooling, no dependency manifest, and no architecture to document yet.

When code is added to this repository, update this file to cover:
- Build, lint, and test commands (including how to run a single test)
- High-level architecture and structure
